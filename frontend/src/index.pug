extends layout

block title
  title Zdzira — Projects

block body
  .app
    include includes/topbar
    main.project-list(
      hx-get='/projects'
      hx-trigger='load'
      hx-swap='innerHTML'
    )
      p.loading Loading projects…
